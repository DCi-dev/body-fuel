interface RecipeCardProps {
  name: string;
  description: string;
  category: string;
  imageSrc: string;
  slug: string;
}


export default function RecipeCard({ name, description, category, imageSrc, slug }: RecipeCardProps) {
  return <></>;
}
